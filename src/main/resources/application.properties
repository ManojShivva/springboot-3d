
spring.datasource.url=jdbc:postgresql://localhost:5432/character_database?useSSL=false
spring.datasource.username=postgres
spring.datasource.password=91825@Manoj1


spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQL10Dialect

#property to craete tables directly
#ibernate is a Java framework. JDBC is a database connectivity tool. Hibernate can perform automatic object mapping. It maps the object model's data to the schema of the database itself with the help of annotations
spring.jpa.hibernate.ddl-auto = update

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
file.upload-dir=./uploads

server.port=8081
